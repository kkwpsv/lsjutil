namespace Lsj.Util.Win32.Structs
{
    /// <summary>
    /// <para>
    /// This struct is just a PLACEHOLDER, which cannot be defined in C#.
    /// </para>
    /// <para>
    /// An extended dialog box template begins with a <see cref="DLGTEMPLATEEX"/> header that describes the dialog box and
    /// specifies the number of controls in the dialog box.
    /// For each control in a dialog box, an extended dialog box template has a block of data
    /// that uses the <see cref="DLGITEMTEMPLATEEX"/> format to describe the control.
    /// The <see cref="DLGTEMPLATEEX"/> structure is not defined in any standard header file.
    /// The structure definition is provided here to explain the format of an extended template for a dialog box.
    /// </para>
    /// <para>
    /// From: https://docs.microsoft.com/zh-cn/windows/win32/dlgbox/dlgtemplateex
    /// </para>
    /// </summary>
    public struct DLGTEMPLATEEX
    {
    }
}
